"""_summary_ = 'Contains functions that setup, extract, transform,
load, and teardown in the main file of the application
"""

def setup():
    """This function will  initialize
    database sessions, tables, fields,
    and relationships in the schema"""  
    
    
def extract():
    """This function will extract the necessary data
    from the database connection and place into the tables
    established and created in the setup function"""
    
def transform():
    """This function will perform a series of transformations on
    the extracted data"""

def load():
    """This function will load all transformed data into the newly
    created data warehouse star schema created during setup"""
    
def teardown():
    """This function will close any connections to the database"""